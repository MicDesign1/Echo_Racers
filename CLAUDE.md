# Animalian: Echo Racers — Claude Code Instructions

You are helping build **Animalian: Echo Racers**, a creature-powered racing game and
sequel to Animalian Manor, set in the same universe. New repo, separate from
animalian-manor. Vibecoded by two kids aged 10–12 (Scarlet: character design and
aesthetics; Gray: kinetic, mechanical gameplay) with adult supervision.

## What This Project Is

The player travels through a portal to the island where the first Animalian came into
being. An ancient civilization (the Wardens) left behind resonance technology —
machines that only work when an Animalian is bonded to them. The creature riding in
the vehicle IS the engine. Players race the Trial Circuits to unseal the island and
reach Uncle Argon, who is stranded there by an ancient artefact.

## Tech Stack

- **Framework:** React (Vite), plain JavaScript
- **Rendering:** Pseudo-3D sprite-scaling racer (OutRun/Slipstream technique — road
  drawn as projected segments on HTML5 Canvas 2D). **No Three.js, no 3D engines, no
  external game libraries.**
- **Data storage:** localStorage, namespaced per profile
- **Hosting:** Cloudflare Pages (deploy paths are CASE-SENSITIVE)
- Local path: E:\KID GAME\Echo_Racers

## Current Status

STATUS: Playable racing game with full loop — grid start with countdown, races vs data-driven AI roster (lap count is per-track: 3 for four of the five circuits, 2 for Long Circuit since it's by far the longest single lap), verified placement, place-first HUD, a sprite-scaled overhead finish banner (brass posts + a sagging cream/gold cloth, drawn through the same projected-sprite path as roadside pillars, over a checkered ground strip replacing the old solid band), results screen (Race Again / Race Setup / Back to Hub), persistence; time-trial behind RACE.mode flag. Minimal auto-attack combat live (symmetric, telegraphed, recoverable). Sampled + synth audio with lifecycle management (race-end fade, full teardown on unmount): manifest-driven samples (per-file gain, retrigger, pitch variation); attack and damage sounds currently play from randomized pools of all available clips (typed selection is a marked future swap), fade-restart race music, win cheer on 1st only. Input: keyboard on desktop, plus touch controls (steering joystick + throttle/drift, manual/auto-accel schemes) for tablet/phone; desktop keyboard unchanged. Difficulty tiers (Cadet/Racer/Ace) and rival count (1–8) as tuning data, resolved through a practice/trial raceMode config (trial is a structural placeholder only). Practice setup screen live (rival count 1–8, difficulty tier selection, track selection, all persisted defaults). Tracks are DATA (src/data/tracks.js): 5 courses — the original (id trial-circuit-1, byte-identical to the pre-refactor hardcoded track) plus 4 new generic-labeled variants (Long/Winding/Highland/Coastal Circuit; no narrative naming — themed Trial Circuits stay Story-Bible-blocked). Each track is one plain object: an ordered `layout` ({type:'straight'|'hill'|'curve', length, dir/strength/dy as needed} — curve sections can override the enter/leave ease-in/out split for exact reproduction, else fall back to tuning.js TRACK_FEEL.curveEnterLeaveFraction), a `palette` (sky/grass/grassAlt/rumble/road — road tone is `roadTone(tone, base)` in engine/colors.js, now accepting a per-track base color instead of one fixed constant), a `roadside` prop config (pillar/stone modulo+remainder+offset — density and spacing per track, e.g. Long Circuit sparse, Winding Circuit dense), and `lapCount`. engine/track.js no longer builds one fixed course at module load — `loadTrack(trackData)` rebuilds the `track`/`trackLength` module bindings (still live ES-module exports, so every existing `import { trackLength, seg }` elsewhere picks up the rebuilt values automatically); RaceTrack.jsx calls it at mount and on Race Again, resolving the active track via data/tracks.js's `activeTrack()` (keyed off the same `RACE.practice/trial.trackId` the difficulty/rivalCount config already used) and merging its palette over the shared COLORS for that render. A real bug found while wiring this: outside verify mode, a direct/refreshed load of /race hydrates difficulty and rivalCount from the last-saved Practice config but had NOT been hydrating trackId — fixed so all three now hydrate together. Regression scripts updated: verify-combat.mjs re-runs its whole suite once per track (combat is gap/position-based, not geometry-based, so this is a real loop, not a smoke test); verify-opponents-render.mjs keeps its full hardcoded exhaustive sweep on the original track (still the byte-identical reference) and adds a lighter track-agnostic smoke test — positions scaled off each track's own actual length — for the 4 new tracks. Manually verified: a full 3-lap race finishes correctly on a non-default track (Winding Circuit) with correct placement/results/best-lap recording keyed to that track's own id. The walkable hub is now the game's HOME (title-screen "Play" → /hub; no dev links): a Canvas-2D tiled forest drawn by a stored-tile engine (engine/tilemap.js) from DATA (src/data/hubMap.js: ground / decor-under / decor-over layers + a walkability grid; spawn + zones). The map is IMPORTED from Mic's Tiled authoring (Hub map-quality rework, step 2, v2 with a lodge): scripts/import-tiled-map.mjs converts the .tmj export (kept outside the repo, in Mic's working folder) into that same plain-data shape — 31×21 tiles, hand-laid-out with a cave mouth + flanking garden wall (top), a proper cliff-and-water shoreline (bottom, built from the atlas's own cap/face/base/pool cliff tiles per docs/tile-grammar.md), and a small lodge building (a Mana Seed "timber" tileset gable, roof + a windowed wall — the sheet's actual door art exists but isn't placed in this export, so the in-game zone sits at the window wall's front rather than literal door art; flagged, not improvised around). The engine now supports MULTIPLE atlases natively: tuning.js HUB.tile.atlases lists each one in order and every tile id lives in ONE unified space spanning all of them concatenated (engine/tilemap.js resolveAtlas()) — this is what let Mic mix forest and lodge tiles in the same decor-over/decor-under layers instead of needing the lodge bolted on as a separate fixed stamp. Walkability is derived from TILE IDENTITY, not layer — see isBlockingId() in the importer; docs/tile-grammar.md is the source of truth and gets extended whenever a new tile region needs classifying, and the importer itself hard-fails on any tile id outside its known categories rather than silently guessing. Re-running the importer after a re-export regenerates hubMap.js from scratch (do not hand-edit the terrain arrays). `hubMap.js`'s `decorUnder`/`decorOver` are each an ARRAY of layers, not one flattened array — Tiled genuinely lets multiple tiles composite with transparency at the same cell across different layers in a group (e.g. a bush's transparent corners revealing a cliff face drawn under it), and collapsing to a single winner-takes-all tile per cell silently destroyed that (a real bug, found and fixed this session); `engine/tilemap.js` draws each sub-layer in sequence via `HubScene.jsx` looping `drawLayer()`, matching Tiled's own compositing. A one-time depth-layer AUDIT (scripts/fix-tmj-depth.mjs, re-runnable for future Tiled cleanups) scans a .tmj for tiles sitting in the wrong group per tile-grammar.md — cliff caps/top edges, tree canopy, and roof-overhang belong in decor-over; cliff faces, trunks, walls, and ground scatter (flowers/pebbles/bushes/boulders) belong in decor-under — and writes a corrected copy (never touches the input; e.g. `hub-fixed.tmj`), moving misplaced tiles into a free slot in the correct group (adding an overflow layer only if every existing layer in that group is already occupied at that cell, e.g. two overlapping trees), flagging true ambiguities (the cave mouth isn't named in either bucket) instead of guessing, and reporting ground-layer holes. Found and fixed 182 misplacements this way (mostly canopy/trunk tiles sitting in the wrong group, which had been silently breaking walk-behind/occlusion for the player and critters); verified via before/after screenshots and a pixel-diff pass that the only rendering changes were the intended occlusion corrections. That pass also caught a WALKABILITY bug via playtesting (not pixels): the cliff family's "rounded grassy cap" (rows 0-3) had been treated as open ground for a hypothetical walkable hilltop, but the atlas only supplies rim/corner art there (the doc's own note says the interior is "mostly transparent... just fill with plain grass" — an actual walkable mound would use plain grass tiles, never these) — Mic's wall beside the cave uses a few of those rim tiles as a decorative top edge, and treating them as walkable let the player climb through/onto the wall there. Fixed: the entire cols 5-10 cliff family (rows 0-15) now blocks with no exception. A camera follows the player and clamps to the map bounds; natural borders (water lake, tree lines) replace invisible walls; tree canopies AND the lodge roof (both decor-over) draw over entities so the player walks behind them. The bottom-anchored Mana Seed player (keyboard WASD/arrows + reused touch joystick, 4-direction walk with animation, facing persists when idle) collides feet-box vs the walkability grid with edge sliding. DATA-DRIVEN interaction zones (in hubMap.js) — "Races" (at the cave mouth; renamed from "Trial Gate") opens Practice, "Lodge" (at the lodge's front wall — the earlier "Mirror"/"Home" placeholder is RETIRED; canon set this session: the timber house is the player's lodge, wardrobe access at its door, deeper identity stays Story-Bible-open) opens avatar customization — plus a persistent Quick Race button; zone labels are drawn with NO trigger-radius circle (removed per Scarlet/Gray's request) — just always-visible, legibly stroked Cinzel text at a separate per-zone `labelTile` anchor the importer places above the cave / below the lodge (distinct from the zone's own walkable trigger tile, since the label anchor is purely visual and doesn't need to be walkable — and is clamped to the map, since the cave sits flush against the top edge with no tile actually above it). Spawn and both zones are placed by the importer on walkable, mutually-reachable cells (verified by a flood-fill reachability check baked permanently into verify-hub-render.mjs). A HubScene.jsx load-time guard falls back to spawn if a profile's persisted position is out of bounds or on a blocked cell, AND (new) if its `mapVersion` (a hash of the terrain the importer bakes into hubMap.js and stamps onto every save) doesn't match the current map's — this was a real bug, not just a guard: re-exporting hub.tmj with "the same elements moved around" left previously-saved positions still nominally walkable-and-uncovered on the new layout while landing somewhere nonsensical (reported as the character spawning stuck against clutter near the map edge); every re-import now invalidates old saves automatically instead of relying solely on walkability. Sub-screens (Practice/Race/Avatar) return via a reload-proof sessionStorage origin (saves.getOrigin/setOrigin) that survives a hard reload. Last hub position persisted per profile; player state kept as ONE plain serializable object. Avatar look is DATA (hub Phase 2): a plain serializable descriptor { body, outfit, outfitColor, hair, hairColor, hat, hatColor } rendered by a palette-swap compositor (engine/avatarComposite.js) that maps official Mana Seed ramps — extracted EXACTLY into src/data/avatarPalettes.js by scripts/generate-avatar-palettes.mjs, style manifest in src/data/avatarManifest.js — onto ONE cached composited sheet per look (body→outfit→hair→hat, once per descriptor, not per frame); customization screen (/avatar) is fully manifest-driven (one picker row per slot) with a live walking preview, per-slot style arrows + ramp-driven color swatches, Randomize, and Save/Cancel; avatar persisted per profile (saves.getAvatar/setAvatar). Hub Phase 1.5 additions: (B) a hat slot with a 'none' (bare-headed) option plus Straw/Pointed hats (5 colors each), drawn last on top of hair; old saved avatars normalize to hat 'none'. (C) ambient wildlife — placeholder, non-interactive critters (free Mana Seed slime, gentle idle-bob only; attack/death frames intentionally unused) with a per-map population (hubMap.critters) scaled to walkable area by the importer via tuning.js HUB.critter.tilesPerCritter (a density constant, not a hardcoded count — calibrated from the original hand-authored map), wander behavior (engine/critters.js: idle → pick a walkable target clear of zones → glide → idle, small feet-box vs the grid so they never enter water/trees/borders, and they pass through the player), sheet config in src/data/critters.js and motion feel in tuning.js HUB.critter; critters + player are depth-sorted by feet-Y and draw under the canopy layer. Regression scripts: verify-opponents-render.mjs, verify-combat.mjs, and verify-hub-render.mjs (asserts map load, walkability, a spawn/zones reachability flood-fill, zones, camera clamp, palette compositing incl. a newly-added hat style, and that critters animate + stay on walkable ground while wandering) — run the relevant ones after render/combat/audio-loop/hub changes. Chassis are placeholder vector art awaiting sprites; hub sprites are the free Mana Seed demo base (placeholder). Dev tool scripts/render-hub-snapshot.mjs renders the full hub map (all tile layers) to tmp/hub-snapshot.png (gitignored) via the real HubScene + its verify hook, for visual sanity-checks. Wardrobe inventory pass (Hub Phase 1.6) against the second free pack ("25.07 Free Mana Seed RPG Starter Pack", extracted at ../assets/manaseed2/ outside the repo) found NO new usable wardrobe content: every char_a_p1 body/outfit/hair/hat file already in public/sprites/hub/layers/ is byte-identical to this pack, and the only two files not yet present (1out/boxr, 1out/undi — both single-color, shirtless-plus-underwear looks) were declined as inappropriate for the wardrobe. The pack's char_a_pONE1/2/3 pages share char_a_p1's 512x512/8x8 dimensions but hold combat animations (draw/sheath, parry, dodge, hurt, dead), not stand/walk, so they don't conform to the current compositor's row assumptions — flagged as future held-item/combat-stance material (their 6tla/7tlb weapon/shield layers + the pack's standalone icon-style weapon sprites are catalogued, not integrated). Its tileset/slime assets are already in use (confirmed byte-identical). Hub Phase 1.7 turned the pack's npc man/woman sheets (previously catalogued as hub-NPC material) into two STANDALONE avatar body options instead — generic "Traveler (Man)"/"Traveler (Woman)" cosmetic looks, no names/lore invented. Confirmed on the actual files (not assumed): 128x256 canvas, content only in the top 128x128, a 4-col x4-row grid of 32x32 frames — row0 down, row1 RIGHT, row2 up, row3 LEFT (row3 is a pixel-perfect mirror of row1 baked into the file; verified with an objective landmark — the eye, the darkest opaque pixel in the frame, sits right-of-center for row1 and left-of-center for row3 — after an initial pass had these two backwards, caught only once actually playtested, not by the render/compositing checks, which is why verify-hub-render.mjs now asserts eye-x directly instead of trusting row labels), 4 walk frames (vs char_a_p1's 6, no separate idle row — col0 doubles as idle); v00 of each is an intentionally-ugly reference palette per the pack's readme, so only v01-v04 (4 colors) are used. These do NOT go through the palette-swap compositor: `avatarComposite.js` short-circuits for `standalone` body styles (data/avatarManifest.js) and caches/draws the variant sheet directly. The descriptor keeps its existing seven fields — no new shape — by compound-encoding the body slot (`"npcMan:c00"`; a plain value with no ':' still means the legacy human skin color, so old saves load unchanged); `data/tuning.js` HUB.npcSprite holds the standalone frame grid/timing/drawScale so nothing is hardcoded — drawScale tracks HUB.player.drawScale directly (NOT doubled despite the frame being half the pixel size: the composited player's actual character only fills about half its 64px frame, while this art fills nearly all of its 32px frame, so the on-screen character sizes already match at the same scale — measured on real pixels, not assumed, after an initial doubled-scale pass rendered ~2x too big). Selecting a standalone body visibly locks (grays out, inert) the outfit/hair/hat rows in /avatar since their look is baked in; Randomize and the live preview both handle either mode. Track length/hill-smoothing/air-time pass: diagnosed the steep-hill "road just vanishes" visual skip as a render-loop bug, not a data bug — segment elevation was already smoothly cosine-eased per layout section, but a short/steep section could still produce a peak per-segment slope sharp enough to misfire RaceTrack.jsx's hill-crest occlusion clip (it mistook a steep continuous downhill for cresting a real hill and truncated draw distance); fixed with a box-blur pass over the final segment-height profile (engine/track.js smoothElevation(), radius/passes in TRACK_FEEL.elevationSmoothing*) that caps per-segment slope regardless of how steep the source layout data is, verified by before/after screenshots through the steepest descent. All five tracks then scaled 2.5x in length (src/data/tracks.js layout `length`/`enter`/`leave`, dy/strength left unchanged so hills read as proportionally gentler climbs over more distance) so a lap reads as a real lap: Circuit One 1488 segments, Long Circuit 2250, Winding Circuit 1263, Highland Circuit 1550, Coastal Circuit 1601. Re-verifying the finish banner (below) surfaced a real pre-existing data bug: three tracks' hill `dy` values didn't sum to zero across their full layout, meaning the track never returned to its starting elevation — a closed lap must — producing a genuine elevation cliff right at the lap seam/finish line that made the finish banner render distorted/off-screen there; fixed by rebalancing each track's closing elevation (Winding's one hill split into an up+down bump, preserving — and actually sharpening — its crest; Highland's closing curve dy -25→-45; Coastal's closing hill dy -10→-6), confirmed via a seam-continuity check (segment 0's y now exactly matches the last segment's y on all five tracks) and re-screenshotted. Hill-crest air time (engine/airtime.js, AIR in tuning.js): cresting a genuinely sharp hill (crestSharpnessAt() — an approximation of the elevation profile's curvature, gated on AIR.crestThreshold, calibrated against each track's real built elevation profile so Long Circuit's gentlest-by-design crest never qualifies) at real speed (AIR.launchSpeedPercent) launches the chassis — a visual-only lift (fraction of carHeight, so it scales with perspective the same way carWidth already does), duration scaling with speed x crest sharpness capped at AIR.airDurationMax, reduced (not zero) steering/accel authority while airborne (AIR.steerLockFactor/accelLockFactor; curve centrifugal pull and drift are suspended outright since neither makes sense with no wheels down), and a damped landing settle/bounce — never a speed penalty, spinout, or damage in either direction. Identical rules drive the player and every AI rival (same symmetric pattern as COMBAT DESIGN); optional polish: a brief engine-hum pitch lift while airborne (AUDIO.engine.airPitchLiftHz), reusing the existing synth hook. Results screen now offers three options instead of two: Race Again (resets the race in place via the existing resetRace() plumbing — no navigation, same track/difficulty/rivalCount), Race Setup (navigates to /practice, which already pre-fills from the just-raced persisted config), Back to Hub (unchanged) — all reload-proof via the existing sessionStorage origin mechanism. New regression script verify-airtime.mjs (launch/no-launch gating at real per-track crest positions, confirms landing always resolves) joins verify-opponents-render.mjs/verify-combat.mjs, all passing on all five tracks after every change in this pass; manually verified a full natural-physics lap (countdown through finish through Race Again reset) end-to-end.

Not yet built: typed attacks + HP, special-move perk, creature/vehicle select, named/themed Trial Circuits (lore-blocked — current 5 tracks are generic-labeled only), roadside allies (lore-blocked), menus, real (non-slime) wildlife + interactive creatures, multi-profile UI (hub Phase 3+), held-item cosmetic slot.

## Narrative Rules — CRITICAL

- The **Sequel Story Bible (Sections 1–3)** is the ONLY source of narrative truth.
  If `Animalian_Sequel_Story_Bible_S1-3.docx` is present in the repo, treat it as
  READ-ONLY canon to consult — never edit it, never extend it.
- NEVER invent story content, character names, item names, place names, or lore. If a
  task needs a detail that isn't in the Bible or already in the codebase, STOP and
  flag it in the session summary as "narrative decision needed." All narrative
  decisions are resolved in the Claude.ai project conversation, never in Claude Code.
- Canon from Game 1: the Masked Man escaped; six types (Ember, Tide, Thorn, Storm,
  Phantom, Iron); legendary dual-types Genesis (Storm/Ember), Rekron (Ember/Iron —
  spelling locked), RZ (Iron/Tide).

## Art Direction — CRITICAL

Victorian naturalist meets ancient Atlantean craftsmanship. Brass, silver, verdigris
copper, river-worn stone, warm parchment skies. Machines glow with soft "resonance light"

**Vehicles are all-metal Warden hovercraft:** brushed silver, brass, verdigris copper
accents. Stone appears only in environments, never on vehicles.

## Color Palette (carried over from Game 1)

| Name      | Hex     | Use                     |
| --------- | ------- | ----------------------- |
| Cream     | #FFF8E7 | Backgrounds, highlights |
| Aged Gold | #C49A3C | Badges, accents         |
| Walnut    | #5C3A1E | Headers, UI bars        |
| Brass     | #8B6914 | Buttons, frames, hinges |

## Typography

- **Display:** Cinzel
- **Body:** Crimson Text / Georgia

## Creature System (carried over from Game 1)

Six types: Ember, Tide, Thorn, Storm, Phantom, Iron.

- Standard type advantage: 1.5x / disadvantage 0.75x
- Phantom: 1.25x vs all / 0.75x taken from all
- Iron: neutral (no advantage/disadvantage either way)
- Legendary dual-types exist: Genesis (Storm/Ember), Rekron (Ember/Iron), RZ (Iron/Tide)

### Card stats → racing stats

| Card stat | Racing meaning        |
| --------- | --------------------- |
| SPD       | Speed / acceleration  |
| ATK       | Attack power          |
| DEF       | Hull toughness        |
| HP        | Damage before spinout |

## Sprite Conventions

- Individual frames only, never sprite sheets
- 512px WebP with alpha, bottom-center aligned, into `public/sprites/`
- Vehicle frame set per craft: rear straight, slight-left, slight-right, hard-left,
  hard-right
- Vehicles support runtime tinting: keep resonance-glow pixels in a distinct color
  range from hull metal so glow and hull can be recolored independently

## Tone & Safety Rails

- Wholesome, never scary.
- Damage slows racers but never eliminates them; vehicles mend after races. No death,
  no explosions-as-destruction.

## Workflow Rules

- ONE change at a time. All changes must be scoped and non-destructive — do not touch
  visual design or working functionality outside the explicit scope of the request.
- End every session with a summary; wait for confirmation before the next change.
- Verify repo state by READING actual files. Never assume.
- Build/lint success ≠ runtime correctness — test the real scenario (run the game,
  drive the road).
- Use `git add -A` for commits.
- Keep tuning constants centralized in `src/data/tuning.js` so game feel can be
  iterated easily. No magic numbers scattered in components.

## COMBAT DESIGN: 
Attacks are automatic — the bonded creature attacks rival racers within range or on bump, governed by a per-creature cooldown. No attack button. All racers' creatures follow the same auto-attack rules. Exception: a once-per-race special move, triggered manually, available as an equippable car perk (future customization system). Roadside wild animalians are allied to one specific rival racer and attack other racers (including NPCs) near their stretch of track — sometimes, not always. Who that rival is = narrative decision, resolve from the Story Bible before naming.